services:
  ollama:
    environment:
      OLLAMA_NUM_THREADS: "8"
      OLLAMA_NUM_PARALLEL: "4"

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]